# Database - PostgreSQL via Docker
# Make sure to run `docker-compose up -d` before using the app
DATABASE_URL="postgresql://bowling_user:bowling_password@localhost:5432/bowling_tournament?schema=public"

# For production, use a secure connection string:
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public&sslmode=require"
